import { FlowState } from '.';
import { TaskResolverMap, ValueMap, FlowStateEnum } from '../../types';
import { SerializedFlowRunStatus } from '../flow-run-status';
export declare class FlowReady extends FlowState {
    getStateCode(): FlowStateEnum;
    start(params: ValueMap, expectedResults: string[], resolvers: TaskResolverMap, context: ValueMap, options?: ValueMap): Promise<ValueMap>;
    getSerializableState(): SerializedFlowRunStatus;
}
